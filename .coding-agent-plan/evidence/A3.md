# A3 evidence

Status: current

- A3 started after G0 removed the last product need for ambient Git execution.
- Agent Core's neutral `CommandExecution` port is being corrected so a command authority prepares exact non-executing enforcement evidence before tool authorization. The prepared evidence becomes part of the canonical tool fingerprint; effect activation remains single-use and post-approval.
- Coding Agent will replace `--apply`, `--dry-run`, and `--allow-shell` with explicit permission modes, omit denied tools, and compose only the Sandbox-backed command authority. No ambient command fallback will remain.
