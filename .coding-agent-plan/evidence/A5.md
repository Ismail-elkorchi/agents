# A5 — Change accounting and review data

Status: current

- A5 starts from Agents `f83aeaa4b8c877c12b69802d493e3778688e819c`, where A4 owns an immutable per-run pre-edit snapshot and every structured mutation already produces exact Agent Core transaction receipts.
- The implementation will derive one coding-domain change set from those authorities. It will not infer change truth from model prose, ambient Git commands, or verifier output, and it will not add rollback semantics deferred by C4.
