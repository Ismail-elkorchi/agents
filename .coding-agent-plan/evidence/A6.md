# A6 — TUI and session correctness

Status: current

- A6 starts from Agents `464279d9c9d2a13f8685e6f790c43e43445bf696`, Agent Core `48b4a2ee5dfc25d31d5093f322378bfd10f33375`, and Terminal UI `d83d01432056470d17f42793bb83f6d7c93e595d` after C3 completed.
- The current TUI already presents live agent progress, approvals, non-approval recovery suspension, checks, terminal truth, permissions, and bounded conversation items. It does not yet hydrate the complete durable session/run state before live delivery.
- The initial source audit confirmed the A6 defects named by the node: dispatch rejection was swallowed, the streaming queue evicted accepted events at an arbitrary count, command effects were partly presentation-only, and durable restore/long-pressure/shutdown coverage was incomplete.
- Durable hydration now projects the exact session replay, pending submissions, operation inspections, terminal checks, persisted workspace change reports, approvals, unknown-effect recovery, queue, and driver control before live delivery. Stable run-bound tool/check identities and assistant upserts prevent restore/live duplication.
- `CodingAgentTuiEventSource` now applies backpressure through Terminal UI's reliable/replaceable source channel. The consumer-side eviction queues, timer batching, and swallowed dispatch failures were removed; source failure becomes an application failure/exit, while cancellation and shutdown drain admitted work explicitly.
- Composer Up/Down history restores the prior draft, and the unused `InteractiveCommandEffect` result branch was removed. Session configuration/queue/compaction and live change reports use ordinary application messages; UI reducers do not execute runtime authority.
- Transcript coverage includes restore then live append, exact approval, unknown effect, recovered operation, terminal checks/changes, 1,100 streaming updates, history, tiny modal resize/focus, cancellation, dispatch failure, active-run exit, and clean shutdown.
- The tiny-modal transcript confirmed a Terminal UI ownership defect: clipping all modal descendants erased the logical focus path and Escape routing. The root framework fix at Terminal UI `01277f04674c5f021cbe93dccfe378bacec7b632` preserves an already-focused non-disabled clipped target without adding it to traversal; all 20 focus tests and the complete Terminal UI release gate passed (1,395 unit tests, Node/Deno/Bun consumers and runtimes, JSR dry-run, and 41 performance contracts). Agents now pins that exact pushed commit.
- Agents implementation commit `8a9d48070f195e76f003732da710eeb3ed448f99` passes the complete local release gate: plan validation, build, lint, 80 unit tests (76 pass, 4 environment skips), and packed consumers. GitHub verification remains to be recorded before completion.
