# A7 — Complete coding product loop

Status: current

- A7 starts from Agents `7f64bb321216f3e394c141d80402762c7d1d3df8`, Agent Core `bfefe709fb544c3324b3aa61e328a4c2737fc94c`, Terminal UI `01277f04674c5f021cbe93dccfe378bacec7b632`, and Sandbox `f323ee5da7518b80c98842fc1d877ef7c5010062` after F0 completed.
- The initial audit confirms that the product already composes repository orientation and scoped instructions, root-bound inspection and structured mutation, no-network Sandbox commands, exact candidate verification, durable disposition/revision, change reports, approvals, operation recovery, sessions, branching, and CLI/TUI progress. Existing resilient CLI coverage proves a confined read/edit/check operation across process loss.
- Agents `dd952e198d3617f35243688ed4da39219f62cae9` completes the first product-loop slice. The coding contract now states the full product-local workflow and makes clear that a local plan is neither runtime authority nor durable operation state. A shared presentation module derives remaining uncertainty only from terminal/check/change evidence; the CLI and TUI use it, including change-only recovery states. Exact change attribution, conflicts, partial coverage, unknown checks, incomplete candidates, and verification that did not run are named rather than hidden behind a generic partial marker.
- TUI hydration now reads durable session branch points and projects a bounded session-history activity with final entry, run identity, execution, verification, and candidate status when the terminal remains in active replay. It explicitly labels branch points whose terminal is outside the active replay instead of inventing state.
- Deterministic tests bind the shared summary to machine facts, the CLI resilient edit path to `Remaining uncertainty: none`, hydrated terminal/change/history presentation to exact prior state, and the coding instructions to the complete workflow. `npm run verify:release` passed with 82 tests (78 passed and 4 environment-gated skips), TypeScript build, lint, plan validation, and packed consumers. The focused resilient CLI suite passed 3 tests with the native Sandbox slice skipped where its Linux namespace backend is unavailable.
- Remaining A7 work is the deterministic real-task matrix for bug diagnosis, confined fix, multi-file refactor, dead-code removal, review-only, denied action, dirty worktree, non-Git root, failed-check repair, concurrent file conflict, malicious repository content, and no-network execution. Interrupted/recovered work is already exercised. The next slice will consolidate reusable scripted-provider fixtures and map every scenario to direct product evidence while removing duplicated fixture code.
