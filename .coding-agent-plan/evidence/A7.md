# A7 — Complete coding product loop

Status: current

- A7 starts from Agents `7f64bb321216f3e394c141d80402762c7d1d3df8`, Agent Core `bfefe709fb544c3324b3aa61e328a4c2737fc94c`, Terminal UI `01277f04674c5f021cbe93dccfe378bacec7b632`, and Sandbox `f323ee5da7518b80c98842fc1d877ef7c5010062` after F0 completed.
- The initial audit confirms that the product already composes repository orientation and scoped instructions, root-bound inspection and structured mutation, no-network Sandbox commands, exact candidate verification, durable disposition/revision, change reports, approvals, operation recovery, sessions, branching, and CLI/TUI progress. Existing resilient CLI coverage proves a confined read/edit/check operation across process loss.
- Agents `dd952e198d3617f35243688ed4da39219f62cae9` completes the first product-loop slice. The coding contract now states the full product-local workflow and makes clear that a local plan is neither runtime authority nor durable operation state. A shared presentation module derives remaining uncertainty only from terminal/check/change evidence; the CLI and TUI use it, including change-only recovery states. Exact change attribution, conflicts, partial coverage, unknown checks, incomplete candidates, and verification that did not run are named rather than hidden behind a generic partial marker.
- TUI hydration now reads durable session branch points and projects a bounded session-history activity with final entry, run identity, execution, verification, and candidate status when the terminal remains in active replay. It explicitly labels branch points whose terminal is outside the active replay instead of inventing state.
- Deterministic tests bind the shared summary to machine facts, the CLI resilient edit path to `Remaining uncertainty: none`, hydrated terminal/change/history presentation to exact prior state, and the coding instructions to the complete workflow. `npm run verify:release` passed with 82 tests (78 passed and 4 environment-gated skips), TypeScript build, lint, plan validation, and packed consumers. The focused resilient CLI suite passed 3 tests with the native Sandbox slice skipped where its Linux namespace backend is unavailable.
- Agents `a644906c060c1935911283f153126dc863b56ac3` closes the workflow defect discovered by the scenario audit: Coding Agent previously inherited the neutral accept disposition, so a proven required-check failure ended with a failed result instead of entering the product's repair loop. The product now supplies a deterministic, implementation-bound policy that revises only proven required failures, includes bounded exact check evidence, explicitly forbids weakening or bypassing the verifier, accepts advisory failures and unknown evidence without pretending they passed, and remains bounded by Agent Core's durable candidate-revision limit.
- The scripted Ollama/CLI process harness moved out of the resilience test into one reusable fixture; the duplicated inline server, process, workspace, trust, and native-Sandbox setup was deleted. Its deterministic controls now also support exact chat-count barriers, queued responses, restricted trust, Git baselines, and process loss.
- The real-task matrix is direct product evidence rather than prompt-only unit coverage: review-only bug diagnosis proves a non-Git root and hostile `AGENTS.md` cannot expose mutation or command tools; restricted mutation denial proves the proposed patch remains unapplied across approval resolution; the existing confined edit proves scoped instructions and interrupted recovery; required-check repair proves a failing exact candidate is revised and then passes the unchanged check; no-network execution targets the live host provider endpoint from the command sandbox and must fail closed; a Git fixture refactors multiple files, deletes dead code, inspects the result, preserves an unrelated dirty file, and surfaces its initial status; and a chat barrier injects a concurrent replacement after the structured receipt so the final report identifies external attribution, receipt conflict, partial coverage, and every remaining uncertainty.
- `npm run verify:release` passed after this slice with 90 tests (84 passed and 6 explicit host-capability skips), TypeScript build, lint, plan validation, and packed consumers. The non-native matrix cases all ran locally; GitHub release CI remains the required Linux namespace execution for the confined command/check, failed-check repair, no-network, and exact dirty-Git observations before A7 can become done.
