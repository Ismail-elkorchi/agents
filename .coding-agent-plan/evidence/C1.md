# C1 evidence

Status: done

- Started from Agent Core `8fbf015dd79156444aa8ad932bbae6a999c75042` after the D0 driver and cancellation boundary passed its focused verification.
- The work must bind prepared tool authority to one operation, driver generation, call identity, and lifetime. It must release every capability after denial, abort, supersession, settlement, or failure without adding a second policy authority.
- Agent Core `6eeba19b86ba477859c7bb3b2747a93955d6bd9f` adds an explicit preparation lifetime. Canonicalization and effect derivation may transfer cleanup-only resources into that lifetime; the resulting preparation is nominal and no longer exposes the raw tool invocation function.
- Invocation requires a one-shot authority created by consuming an E0 effect-start ticket whose implementation ID, canonical parameter digest, and driver generation match the preparation. Stale generations, mismatched tickets, forged values, double transfer, and repeated invocation fail closed.
- Batch orchestration registers each preparation immediately after creation. Denial, approval suspension, recovery deferral, authorization or persistence failure, abort, successful observation persistence, and tool failure all release exactly once. Approval revalidation releases its temporary preparation as well.
- Cleanup failures are aggregated with the primary failure instead of replacing it. Tests that previously called the deleted raw `prepared.invoke` path now use the same one-shot authority boundary as runtime consumers.
- This node deliberately does not claim durable cross-process effect settlement. The current ticket is bound to the durable D0 driver generation, but D2 must persist per-call intent, start authority, settlement permit, and takeover reconciliation before a live stale process can be excluded from concurrent external execution.
- Verification: `npm run verify:release` passed at `6eeba19b86ba477859c7bb3b2747a93955d6bd9f`, including 325 unit tests, 150 focused tests, lint, build, and packed-consumer declaration/runtime checks.
