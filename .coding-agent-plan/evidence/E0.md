# E0 evidence

Status: done

- Started from Agent Core `44c5e19b85a755609697b36b83c36066a021df25` after R0.
- Agent Core implementation commit: `50749d7eeb9626e936dd3522c7d371ea5f9b36b8`.
- Added the lower-level `@agent-core/effects` domain contract shared by models, tools, and runtime. It captures preconditioned re-execution, query/reconciliation identity and expiry, parameter-bound idempotency with expiry, journal-backed mutation reconciliation, and explicit unknown recovery.
- Replaced `pure | idempotent | non_idempotent` in `ToolEffects`; resource accesses and locks remain the sole scheduling-conflict inputs.
- Current built-in tools declare `unknown` because the existing call boundary does not yet capture the preconditions, journal receipt, or durable execution identity needed to prove a stronger capability. The old runtime consequently refuses every incomplete start rather than replaying from a label.
- Added generation-fenced one-shot start tickets, exact-effect settlement permits, terminal/late settlement results, explicit closure reasons, and conservative unknown-exposure retention.
- Provider adapters may expose request-specific recovery facts; absence is explicitly unknown. Current adapters make no unsupported claim.
- `npm run verify:release` passed: 315 unit/security/property tests, 146 focused tests, lint/build, and packed consumer checks including the new effects package.
