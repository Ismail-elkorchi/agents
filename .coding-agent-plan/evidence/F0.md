# F0 — Fault, process-kill, and live stale-owner conformance

Status: done

- F0 starts from Agents `8a9d48070f195e76f003732da710eeb3ed448f99`, Agent Core `48b4a2ee5dfc25d31d5093f322378bfd10f33375`, Terminal UI `01277f04674c5f021cbe93dccfe378bacec7b632`, and Sandbox `e82ff3dd94a7c87b14bd25665b8898ae3b081767` after A6 completed.
- The pre-implementation audit found existing phase coverage for JSONL append/index/torn-tail recovery and cross-process locking; provider intent, uncertain completion, persisted settlement, and takeover; every parallel settlement permutation and source-order projection; approval and tool process-death recovery; verifier reconciliation; disposition recovery before, after, and across its state transition; finalization faults after prepare, session projection, and `run.ended`; and durable session restore. The missing boundaries were operation acceptance/driver-claim faults for both repositories, live old-driver/new-driver processes at the required pause points, process death around submission completion, persistent quarantine after a historical integrity failure, and Sandbox death after its terminal receipt but before state publication.
- Agent Core `bfefe709fb544c3324b3aa61e328a4c2737fc94c` adds ordinary discovery for package conformance tests and a memory/JSONL matrix that injects before- and after-commit faults at operation acceptance and driver claim. Reopen produces only the exact unaccepted/accepted or generation-zero/generation-one state, and replacement ownership advances exactly one generation.
- The same commit makes an integrity failure a durable per-run quarantine, discards derived indexes, and checks the marker before later reads, writes, claims, or runtime work. A historical same-size checksum corruption is audited, a fresh repository instance remains quarantined, and `AgentRuntime.resume` makes zero provider calls.
- Three real child-process tests keep the old JSONL-backed driver alive while paused before ticket start, inside a queryable effect, and after the external completion receipt. A replacement process takes over without concurrent replay; releasing the old process exercises its late transition in every case. The suite proves one external invocation, one `tool.started`, one `tool.ended`, one observation projection, one `run.ended`, an intact ledger, and exact late-permit settlement at most once.
- Two additional child-process tests terminate before and after the durable `submission.completed` commit. Restore stays read-only; waiting for idle resumes claimed work once only in the before-commit case, never replays the after-commit case, leaves no pending submission, and retains exactly one completion record.
- The audited existing matrix remains part of the release gate: `ledger.test.js` covers committed-prefix/torn-tail/index/cross-process append behavior; `operation-driver.test.js` covers fencing, exact stale-owner permits, and all parallel completion permutations; `agent-core.test.js` covers provider/check/disposition/approval/tool/finalization boundaries, including faults after every finalization write and process crashes around tool start, completion, audit, and projection; `session.test.js` covers queued/claimed lifecycle recovery and terminal projection idempotence.
- Sandbox `f323ee5da7518b80c98842fc1d877ef7c5010062` fixes the underlying receipt/state gap: when an unreachable worker has already synchronized a valid result-and-cleanup receipt plus its output chain, inspection atomically publishes the terminal state and returns the exact receipt instead of inventing an unknown outcome or rerunning the command. Corrupt or missing receipts still fail closed. The conformance test recreates the real post-receipt/pre-state crash boundary and proves exact output, completed cleanup, and durable settlement after reopen.
- Agent Core's complete local `npm run verify:release` passed 357 ordinary/conformance tests, 165 focused tests, build, lint, and packed consumers with `exactOptionalPropertyTypes` both enabled and disabled. Its release-gate run `33176585912` passed on Ubuntu current/minimum Node, macOS, and Windows.
- Sandbox host-available checks passed TypeScript build, 74 TypeScript tests (16 pass and 58 native-capability skips), all host Rust workspace tests, packed-consumer tests, license/unsafe/supply-chain audits, and left generated state clean. The local host lacks the musl target plus `rustup`, `rustfmt`, and `clippy`; quality workflow `33176575020` supplied those toolchain capabilities and passed Linux, macOS, Windows, Rust workspace tests, package/audit/supply-chain gates, and every required trust-boundary fuzz target. A later Q0 log audit corrected the original native claim: its Ubuntu job also skipped 58 namespace-dependent tests, including terminal-receipt recovery, because the functional backend probe reported unavailable. Q0 therefore retains native Linux qualification rather than treating that green workflow as executed Sandbox conformance.
- Agents passes its complete local release gate against the exact reviewed dependency commits: plan validation, build, lint, 80 unit tests (76 pass and four environment-capability skips), and packed consumers.
- Agents commit `7f64bb321216f3e394c141d80402762c7d1d3df8` pins the exact Agent Core and Sandbox revisions and passed release-gate run `33177396779` on Ubuntu current/minimum Node, macOS, and Windows. F0 is complete with no contradictory terminal truth, duplicate effect, unsafe replay, missing settlement, invented success, unbounded recovery, or ambiguous next action in the covered matrix.
