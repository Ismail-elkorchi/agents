# G0 evidence

Status: current

- Started after S0 proved the durable Sandbox execution authority and Coding Agent adapter. This node replaces ambient Git subprocess observation with the same fail-closed execution boundary before A3 makes Sandbox the product command authority.

- A2 established that an in-process configuration deny-list cannot make Git status non-executing. Implementation starts only after S0 proves the sandbox execution/receipt boundary.
