# L0 evidence

Status: done

- CI correction `03de5d78c0df530e01f42e8d2b9bbd6259a29547` makes the release workflow materialize the complete authoritative Agents history and the exact reviewed Agent Core and Terminal UI repositories before plan validation. The validator also rejects drift between the reviewed heads and the package pins, so source ancestry and consumer revisions are proved from the repositories actually used by the build rather than from local checkout accidents.
- Follow-up `ae4f5cd30b3620bc9369e468b90e7df573ae74a4` adds the reviewed Sandbox checkout and validation that `verify-source-dependencies` already required. Generated-summary freshness now normalizes checkout CRLF to canonical LF before comparison, preserving exact content validation without making Windows line-ending policy a false source change.
- Follow-up `13c1e1c8c3c0a237e5d60675c35d71561e99719a` builds Sandbox's TypeScript package before the clean Agents consumer build. A source checkout is not mistaken for a consumable package whose public entrypoint is generated under `dist/`.

- Ledger bootstrap commit: `76063583fb4732febf56e83bec09bfd2ccf69306`.
- Implementation commit: `6e74b468aaab8637b2050563a2705fb3b6cb9281`.
- Split records generated from the reviewed ledger without changing its accepted architecture.
- `npm run check:plan`: 26 nodes valid; ten accepted/rejected fixtures passed.
- `npm run plan:context -- L0`: produced a bounded 384-line context from architecture, node, decisions, heads, and applicable instructions.
- `npm run verify:release`: build, lint, 28 package tests, and packed-consumer verification passed.
- The validator rejects invalid statuses, duplicate IDs/evidence, missing dependencies, impossible waves, overlapping parallel/current path authority, duplicate current ownership, missing required evidence, and stale source paths.
- Source-head qualification: `agents` may advance beyond the recorded source head only through the ledger infrastructure paths enumerated by the checker; `agent-core` and `terminal-ui` must equal their recorded heads until the coordinator updates them after a verified implementation commit.
