# Q0 — Deterministic conformance and release gate

Status: current

- Q0 starts from Agents source `a644906c060c1935911283f153126dc863b56ac3`, Agent Core `bfefe709fb544c3324b3aa61e328a4c2737fc94c`, Terminal UI `01277f04674c5f021cbe93dccfe378bacec7b632`, and Sandbox `f323ee5da7518b80c98842fc1d877ef7c5010062` after A7, F0, and T1 completed.
- Existing repository gates already cover scripted providers, controlled tools, fault injection and process kills, hostile filesystem/trust/egress fixtures, Sandbox conformance, TUI transcripts, packed consumers, and cross-platform builds. Q0 must turn the A7 deterministic task evidence into explicit metric inputs and exact zero-violation thresholds, then prove every recorded repository gate at its reviewed head. Platform-specific unavailable guarantees must remain visible evidence rather than silently counted as passes.
- Agents `d13447ed613dfa9dcdab1bb21aa10405c3894dcb` adds an executable Coding Agent conformance gate to the normal `verify` path. Its native deterministic task binds allowed `src/note.txt` and forbidden `untouched.txt` paths, one exact `apply_patch` approval, the required `note-value` check, process death before provider generation, and the exact completed/complete/passed/model-completed terminal. It resumes the accepted operation, suspends before the approved mutation, completes through the real CLI and native Sandbox, and compares the printed summary to the persisted bounded change report.
- A separate underspecified task must request the exact target and acceptable blast radius before mutation. The evaluator records explicit numerators, denominators, and rates for instruction compliance, approval precision, truthful summaries, unnecessary changed paths and bytes, safe clarification, and runs with target/scope violations. Exact thresholds are `1`, `1`, `1`, `0`, `0`, `1`, and `0`; missing expected approvals, extra approvals, terminal drift, failed required checks, wrong kill position, summary/report contradictions, unnecessary changes, contradictory path scopes, and any forbidden-path or reported authority violation fail the gate.
- Unsupported native guarantees do not become skipped passing tasks. The executable emits a structured `unavailable` result naming `linux-namespace-v1`, the platform, the reason, and `metrics: not_measured`; Ubuntu CI must instead emit `verified` after executing the native task.
- The complete local Agents `npm run verify:release` gate passed at this source: plan validation, TypeScript build, lint, four conformance-evaluator tests, explicit local namespace unavailability, 90 product tests (84 passed and six capability skips), and the packed-consumer test. GitHub Linux execution and the exact cross-repository release-run records remain required before Q0 completes.
