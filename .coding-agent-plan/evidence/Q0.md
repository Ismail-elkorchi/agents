# Q0 — Deterministic conformance and release gate

Status: current

- Q0 starts from Agents source `a644906c060c1935911283f153126dc863b56ac3`, Agent Core `bfefe709fb544c3324b3aa61e328a4c2737fc94c`, Terminal UI `01277f04674c5f021cbe93dccfe378bacec7b632`, and Sandbox `f323ee5da7518b80c98842fc1d877ef7c5010062` after A7, F0, and T1 completed.
- Existing repository gates already cover scripted providers, controlled tools, fault injection and process kills, hostile filesystem/trust/egress fixtures, Sandbox conformance, TUI transcripts, packed consumers, and cross-platform builds. Q0 must turn the A7 deterministic task evidence into explicit metric inputs and exact zero-violation thresholds, then prove every recorded repository gate at its reviewed head. Platform-specific unavailable guarantees must remain visible evidence rather than silently counted as passes.
- Agents `d13447ed613dfa9dcdab1bb21aa10405c3894dcb` adds an executable Coding Agent conformance gate to the normal `verify` path. Its native deterministic task binds allowed `src/note.txt` and forbidden `untouched.txt` paths, one exact `apply_patch` approval, the required `note-value` check, process death before provider generation, and the exact completed/complete/passed/model-completed terminal. It resumes the accepted operation, suspends before the approved mutation, completes through the real CLI and native Sandbox, and compares the printed summary to the persisted bounded change report.
- A separate underspecified task must request the exact target and acceptable blast radius before mutation. The evaluator records explicit numerators, denominators, and rates for instruction compliance, approval precision, truthful summaries, unnecessary changed paths and bytes, safe clarification, and runs with target/scope violations. Exact thresholds are `1`, `1`, `1`, `0`, `0`, `1`, and `0`; missing expected approvals, extra approvals, terminal drift, failed required checks, wrong kill position, summary/report contradictions, unnecessary changes, contradictory path scopes, and any forbidden-path or reported authority violation fail the gate.
- Unsupported native guarantees do not become skipped passing tasks. The executable emits a structured `unavailable` result naming `linux-namespace-v1`, the platform, the reason, and `metrics: not_measured`; Ubuntu CI must instead emit `verified` after executing the native task.
- The complete local Agents `npm run verify:release` gate passed at this source: plan validation, TypeScript build, lint, four conformance-evaluator tests, explicit local namespace unavailability, 90 product tests (84 passed and six capability skips), and the packed-consumer test. GitHub Linux execution and the exact cross-repository release-run records remain required before Q0 completes.
- Agents release run [33181645480](https://github.com/Ismail-elkorchi/agents/actions/runs/33181645480) passed all four jobs but was not accepted as native evidence: its Ubuntu logs emitted `status: unavailable` and `metrics: not_measured`, while the six capability-dependent product tests were skipped. A retrospective log audit also corrected the earlier A7 and F0 evidence: Agents run `33179934919` skipped the same six tests, and Sandbox run `33176575020` skipped 58 native Linux tests. A green workflow is not evidence for a task the host did not execute.
- Ubuntu 24.04's official release notes document its default AppArmor restriction on unprivileged user namespaces and the one-boot `kernel.apparmor_restrict_unprivileged_userns=0` control. Agents `6d7f02b6bd01e0814fe51efa5a8065ed7256cf4f` binds both Linux jobs to Ubuntu 24.04, applies that control only to the ephemeral CI host, and requires native conformance through `CODING_AGENT_REQUIRE_NATIVE_CONFORMANCE=1`. The product still probes every mechanism, keeps the complete Linux baseline requirements, and fails closed; an unavailable Linux backend now makes the CI job fail after emitting the structured unavailability record. Sources: [Ubuntu 24.04 release notes](https://documentation.ubuntu.com/release-notes/24.04/#unprivileged-user-namespace-restrictions), [Ubuntu AppArmor security documentation](https://documentation.ubuntu.com/security/docs/security-features/privilege-restriction/apparmor/#apparmor-unprivileged-user-namespace-restrictions).
- Run [33182363539](https://github.com/Ismail-elkorchi/agents/actions/runs/33182363539) proved the qualification works: current Node executed the native conformance task with exact rates `5/5`, `1/1`, `2/2`, `0/1`, `0/5`, `1/1`, and `0/2`, and the previously skipped namespace-backed product cases ran. The minimum-Node job then exposed a lifecycle defect in the Sandbox adapter: it reused the target command's one-second wall-time limit as the deadline for the detached execution host to publish its first observation. Infrastructure activation and target execution are distinct clocks.
- Agents `559cb6f6ccbd44a0b30cedc8d1c22d0e298eec85` gives activation its own bounded 30-second observation deadline without changing the target wall-time enforced by Sandbox. A deterministic delayed-activation test holds the first observation for 1.1 seconds while the target limit is one millisecond, proving the adapter no longer conflates them. The complete local release gate passed with 91 tests (85 passed and six explicit local capability skips), build, lint, plan validation, conformance evaluator, and packed consumers. A green replacement cross-platform run remains required.
- Agents run [33183009775](https://github.com/Ismail-elkorchi/agents/actions/runs/33183009775) is the green replacement: both Ubuntu jobs emitted `status: verified` and exact rates `5/5`, `1/1`, `2/2`, `0/1`, `0/5`, `1/1`, and `0/2`; 90 of 91 product tests passed and the sole skip was the negative secure-root-unavailability case on a host where that authority was available. macOS and Windows emitted explicit `unavailable`/`not_measured` records.
- Sandbox `bf15a9b7356b7547564352c5f027bcb6f2e82f42` adds a stable Linux backend qualification before its native suite and configures the ephemeral Ubuntu host for the documented namespace policy. Run [33183568448](https://github.com/Ismail-elkorchi/sandbox/actions/runs/33183568448) proved the backend available, then exposed one shared detached-execution defect in five formerly skipped cases: accepted activation authority was acknowledged while the durable state still said `prepared`. Q0 now owns the repository state machine, worker, guide, and focused test so activation becomes a durable, fail-closed lifecycle transition rather than a timing workaround.
- Sandbox `a2385c252526391627c6529a406f659fd1803fe5` adds an internal durable `activating` record before acknowledging the exact policy and execution digests. The state cannot regress to `prepared`, cannot be forgotten, and becomes an explicit unknown outcome if the execution host is lost before publishing a process identity. Activation is rejected before preparation, concurrent identical activation calls share the durable acknowledgement, and the detached worker now clears its obsolete preparation-expiry timer. TypeScript build, the Rust workspace suite, 17 locally available JavaScript tests, license audit, and unsafe audit pass.
- Strict run [33184341245](https://github.com/Ismail-elkorchi/sandbox/actions/runs/33184341245) passed backend qualification and all five detached-execution cases that originally exposed the activation race. It then found that the receipt-expiry test configured a 20 ms retention window and assumed a detached process could both finish and be observed inside it; the receipt had correctly expired after 151 ms. Sandbox `c5c3f63f64fe820a7a43be7b5aaeee1ad252d709` removes wall-clock scheduling from that test by publishing explicit settled and expired record deadlines, while preserving the product retention contract. Replacement strict run [33184513712](https://github.com/Ismail-elkorchi/sandbox/actions/runs/33184513712) is pending.
- Agents run [33184555384](https://github.com/Ismail-elkorchi/agents/actions/runs/33184555384) passed the minimum-Node native lane but its current-Node lane received a structured Sandbox `runtime-failure` in the adapter conformance case. The adapter reduced that receipt to `status: failed` and discarded the native error, preventing diagnosis. Agents `b1226f2cadf7065ee6270fe022a2b9a4ce82c78f` preserves the Sandbox runtime error alongside any cleanup failure and adds deterministic boundary coverage. Local build, lint, the ten-test adapter suite, and the complete 91-test release gate pass; the next native run must either pass or expose the exact launcher diagnostic.
