# Q1 — Repeated real-model product evaluation

Status: current

- Q1 starts from Agents `b1226f2cadf7065ee6270fe022a2b9a4ce82c78f`, Agent Core `bfefe709fb544c3324b3aa61e328a4c2737fc94c`, Terminal UI `01277f04674c5f021cbe93dccfe378bacec7b632`, and Sandbox `c5c3f63f64fe820a7a43be7b5aaeee1ad252d709` after Q0 completed with strict native and cross-platform evidence.
- The initial campaign will use three independent runs per task. Mixed per-task outcomes trigger expansion to ten runs before a regression conclusion. Development and holdout sets are versioned separately; holdout task bodies are disclosed to the campaign runner only after the evaluated product, prompt, tools, and policy revisions are fixed.
- Every run record must repeat-bind the provider implementation, exact model revision identity and immutability disclosure, prompt and tool-contract input hashes, policy/configuration hash, Sandbox probe/backend evidence, all repository commits, task content hash and split, repetition, exact outcome, provider token usage, cost basis, elapsed time, machine-grader identity, and human-audit disposition. Campaign metadata alone cannot stand in for a missing run binding.
- Binary distributions use 95% Wilson score intervals and retain every individual outcome. Quality regression requires like-for-like dimensions; a decline of at least 0.15 with non-overlapping Wilson intervals is actionable, while smaller samples are reported as insufficient evidence. Any machine-observed forbidden mutation is reported individually and remains a Q0 deterministic invariant rather than being adjudicated by a model grader.
- Deterministic graders own file identities, allowed/forbidden paths, terminal facts, and recovery evidence. Human/model disagreement is recorded as `disputed`, never silently averaged; the audit expands to every run of that task version. The initial human sample is stratified across both splits and pass/fail outcomes, with at least 20% of runs and a minimum of eight records. Q1 cannot complete while that sample is merely pending.
- Real-model execution is an explicit campaign command and is never part of blocking CI. CI will validate the current-only record schema, task corpus, deterministic grader, distribution calculations, and report reproduction without contacting a provider.
- Agents `6becb60221022dceb48ec801cec367c8500b500a` established the explicit campaign boundary. Eleven current-only task fixtures cover every required category across six development and five holdout tasks. The runner fixes clean repository commits and provider/model/prompt/tool/policy/Sandbox hashes before reading holdout bodies, exercises the public CLI in disposable Git repositories, extracts durable token usage and terminal/change-report identities, expands mixed outcomes, selects the audit sample, and publishes JSON plus a reproducible Markdown report.
- Early local Ollama smokes were diagnostic only and are not accepted Q1 evidence. They exposed insufficient human-audit evidence, a permissive lexical grader, unbound inference settings, and abandoned-generation lifecycle contamination. The resulting provider-independent audit and lifecycle corrections remain; the superseded Ollama campaign, proxy, model-discovery, and unload code has been deleted instead of retained as compatibility code.
- Agents `427a79fc43477395f55723c7398c1bf1081e844b` corrects that record boundary before the official campaign. Machine and adjudicated outcomes are separate. Versioned response evidence uses all-terms alternatives instead of accepting any broad word. Every run retains a bounded final CLI excerpt in a digest-bound evidence artifact, while the current human sample is an immutable digest-named subset. Exact attested decision artifacts must cover all and only pending runs. A disagreement records `disputed`, preserves the machine result, expands evidence to every run of the task version, and leaves the campaign pending until the expansion is reviewed. Existing output directories cannot be overwritten.
- Agents `0fe3aa3ceb6d2a236952a37fe77d892912419c45` binds the current campaign to the already authenticated `openai-codex` provider and exact `gpt-5.6-luna` model. It verifies stored OAuth presence through the public CLI and uses direct ChatGPT-subscription HTTP SSE requests for normal tasks. Reasoning effort defaults to explicit `low`; the exact effort, transport, and application timeout are policy-bound. Unsupported Platform API parameters and all retired Ollama paths are absent.
- The first live recovery smoke exposed an invalid evaluator assumption: an issued provider effect whose caller dies is intentionally `provider_outcome_unknown`, so taskless resume must not silently send a fresh request. Agents `6aa8d73b2a8b4779ea919ce9cff42258d98c8242` corrects the task contract instead of weakening product recovery. A memory-only loopback sentinel holds exactly one initial generation request without forwarding or retaining its body, the process is killed, and resume must exit with an explicit recovery decision while the recorded generation-request count remains one. The unused upstream-forwarding path was deleted. The holdout task is version 2 because its expected semantics changed before the official campaign.
- A first full Luna campaign completed 47 runs after two mixed-outcome expansions, with no forbidden mutation, timeout, or unavailable provider outcome. It is diagnostic rather than accepted: eight correct safe refusals were rejected only because `can't` was not normalized to `cannot`; three valid clarification requests exposed incomplete development evidence alternatives; and four refactor runs consistently retained `deadValue` because the development prompt did not name the export that its exact-file oracle expected removed. The audit artifact also retained only the mismatching file hash, not bounded content a human could adjudicate. Its artifacts were moved intact to `/tmp/coding-agent-eval-diagnostic-2026-08-28-gpt-5.6-luna-r3-low`; no diagnostic rate is treated as Q1 product evidence.
- Agents `f15e32e4f948e98fb634411c0facbe0222f3d273` fixes those evaluator roots before another campaign. Evidence matching now normalizes common negative contractions without loosening all-terms alternatives. The two affected development tasks advance to version 2: the refactor prompt names `deadValue` and `src/legacy.js` exactly, while the under-specified task accepts semantically valid requests for missing expected or failing behavior. Every audit entry now includes path-sorted, bounded, digest-checked observations of all task-relevant synthetic files; unavailable infrastructure never fabricates absent-file evidence. The holdout prompt and expectation are unchanged.
- The subscription route exposes the provider alias `gpt-5.6-luna`, but no dated immutable snapshot. Records therefore bind that exact alias, disclose `immutable: false`, and hash the Agent Core Codex model-profile and request-adapter sources rather than inventing an immutable model digest. The ChatGPT subscription also exposes no marginal per-run price, so cost is recorded as unknown instead of borrowing Platform API pricing. The authoritative model capability reference is [OpenAI — GPT-5.6 Luna](https://developers.openai.com/api/docs/models/gpt-5.6-luna).
- Eight deterministic evaluator tests prove corpus coverage, forbidden-path grading independent of prose, conservative recovery without provider replay, semantic negative-contraction normalization, Wilson distributions with non-measured outcomes, stratified human sampling, report reproduction, current-only field rejection, observed-file and evidence-digest integrity, exact decision coverage, disagreement expansion, and the pending-to-complete lifecycle. The complete local release gate passes with 100 tests (94 passed and six explicit local capability skips), build, lint, Q0 conformance evaluator, plan validation, and packed consumers. Accepted real-model results and the required human audit remain outstanding.
- Agents `0fc6c8f4a385967a11252f498344dbfbc88c3e71` publishes the accepted machine-graded campaign under `coding-agent/evals/results/2026-08-28-gpt-5.6-luna-r3-low-v2/`. It evaluates Agents `ef1c2eb84d886a1961d3d0a0ab2119063e4200a2`, the fixed Agent Core/Sandbox/Terminal UI heads above, authenticated `gpt-5.6-luna`, HTTP SSE, `low` reasoning, and a 240-second timeout. Forty runs completed after the under-specified development task expanded to ten: 39 passed and one failed, for a 97.5% measured pass rate and 87.1%–99.6% 95% Wilson interval. Development was 24/25 (96.0%); holdout was 15/15 (100%). There were zero forbidden mutations, inconclusive outcomes, unavailable provider outcomes, or timeouts. The one failure exhausted the fixed 12-tool budget without producing a candidate and changed no files; the other nine runs of that task passed. Recorded usage totals 796,606 prompt tokens, 22,074 completion tokens, and 818,680 tokens overall across 858,010 milliseconds.
- The campaign, full evidence, sample, and report all reproduce and validate. The initial human sample contains eight records, exactly 20% of the campaign, stratified across both splits and pass/fail outcomes. Its immutable artifact is `audit-samples/84a0f1c3b8178dd322b6ca1835f91abd0149a2507cf30aabb7334cd2f44375a3.json`; the full evidence digest is `sha256:acd08433663fded7e92f65dddfd8e50b7536983f2621fff3d30e45fbfd5130f7`. A person has not yet supplied the required attested decisions, so audit status and Q1 remain `pending`/`current`; no AI-generated attestation is accepted.
