# Q1 — Repeated real-model product evaluation

Status: current

- Q1 starts from Agents `b1226f2cadf7065ee6270fe022a2b9a4ce82c78f`, Agent Core `bfefe709fb544c3324b3aa61e328a4c2737fc94c`, Terminal UI `01277f04674c5f021cbe93dccfe378bacec7b632`, and Sandbox `c5c3f63f64fe820a7a43be7b5aaeee1ad252d709` after Q0 completed with strict native and cross-platform evidence.
- The initial campaign will use three independent runs per task. Mixed per-task outcomes trigger expansion to ten runs before a regression conclusion. Development and holdout sets are versioned separately; holdout task bodies are disclosed to the campaign runner only after the evaluated product, prompt, tools, and policy revisions are fixed.
- Every run record must repeat-bind the provider implementation, immutable model digest, prompt and tool-contract input hashes, policy/configuration hash, Sandbox probe/backend evidence, all repository commits, task content hash and split, repetition, exact outcome, provider token usage, local-cost basis, elapsed time, machine-grader identity, and human-audit disposition. Campaign metadata alone cannot stand in for a missing run binding.
- Binary distributions use 95% Wilson score intervals and retain every individual outcome. Quality regression requires like-for-like dimensions; a decline of at least 0.15 with non-overlapping Wilson intervals is actionable, while smaller samples are reported as insufficient evidence. Any machine-observed forbidden mutation is reported individually and remains a Q0 deterministic invariant rather than being adjudicated by a model grader.
- Deterministic graders own file identities, allowed/forbidden paths, terminal facts, and recovery evidence. Human/model disagreement is recorded as `disputed`, never silently averaged; the audit expands to every run of that task version. The initial human sample is stratified across both splits and pass/fail outcomes, with at least 20% of runs and a minimum of eight records. Q1 cannot complete while that sample is merely pending.
- Real-model execution is an explicit campaign command and is never part of blocking CI. CI will validate the current-only record schema, task corpus, deterministic grader, distribution calculations, and report reproduction without contacting a provider.
- Agents `6becb60221022dceb48ec801cec367c8500b500a` implements the explicit campaign boundary. Eleven current-only task fixtures cover every required category across six development and five holdout tasks. The runner fixes clean repository commits and provider/model/prompt/tool/policy/Sandbox hashes before reading holdout bodies, exercises the public CLI in disposable Git repositories, supports application death before provider generation through a forwarding Ollama proxy, extracts durable token usage and terminal/change-report identities, expands mixed outcomes, selects the audit sample, and publishes JSON plus a reproducible Markdown report.
- A preliminary one-run `gemma4:e2b` diagnosis smoke reached the public CLI terminal successfully and recorded provider usage, but it was intentionally discarded: hashes alone did not give a human enough evidence to adjudicate the sampled answer, and a flat any-keyword rule could misclassify a semantically precise diagnosis. No result from that deficient record format is accepted as Q1 evidence.
- Agents `427a79fc43477395f55723c7398c1bf1081e844b` corrects that record boundary before the official campaign. Machine and adjudicated outcomes are separate. Versioned response evidence uses all-terms alternatives instead of accepting any broad word. Every run retains a bounded final CLI excerpt in a digest-bound evidence artifact, while the current human sample is an immutable digest-named subset. Exact attested decision artifacts must cover all and only pending runs. A disagreement records `disputed`, preserves the machine result, expands evidence to every run of the task version, and leaves the campaign pending until the expansion is reviewed. Existing output directories cannot be overwritten.
- A second clean-commit smoke validated the new campaign, evidence, and sample graph and correctly recorded a provider timeout as `inconclusive`. It also exposed that killing the application did not cancel the proxy's upstream generation, so the harness could remain alive after publishing. Agents `3779c41203c294c88d9f9a40ac7dc8dd6b686dc3` fixes the lifecycle root: client disconnect and campaign shutdown abort every tracked upstream request and close proxy connections. The timed-out smoke is diagnostic only, not an accepted official campaign.
- Both locally available tool-capable model families advertise optional thinking, and leaving it to the provider default made inference mode an unbound, slow variable. Agents `c789e9daead8998ac7380c137e532f03885bc1fc` first bound inference controls; a follow-up smoke showed that model profiles expose portable `enabled`/`disabled` reasoning strategies rather than universal effort levels. Agents `7f351039c80d8cbdcd19ba17e21f88987720fbf4` removes the invalid effort surface. The current campaign schema records reasoning mode, temperature, maximum output tokens, and application timeout in campaign metadata and every run's policy/configuration digest. Diagnostic runs made before that current binding are not accepted results.
- Six deterministic evaluator tests prove corpus coverage, forbidden-path grading independent of prose, Wilson distributions with non-measured outcomes, stratified human sampling, report reproduction, current-only field rejection, evidence-digest integrity, exact decision coverage, disagreement expansion, and the pending-to-complete lifecycle. The complete local release gate passes with 98 tests (92 passed and six explicit local capability skips), build, lint, Q0 conformance evaluator, plan validation, and packed consumers. Real-model results and the required human audit remain outstanding.
- The available local candidate selected for the first campaign is Ollama `gemma4:e2b`, immutable digest `sha256:7fbdbf8f5e45a75bb122155ed546e765b4d9c53a1285f62fd9f506baa1c5a47e`. The campaign record, not this planning note, remains authoritative for the exact evaluated binding.
