# R0 evidence

Status: done

- Started from Agent Core `94a94ce36b11b34d14a2ab807dabbe958933b3a0`.
- Implementation commit: `f3b182c3bb01b4b7f18eeb95229796da1877a21c`.
- Added exact tail `{ sequence, hash, driverGeneration }`, conditional append, run-scoped payload-bound idempotency, bounded `tail()`/`latest()` recovery, and tagged committed/already-committed/rejected/not-committed/committed-index-unknown/outcome-unknown results.
- JSONL remains the authority. Per-run tail and deterministic idempotency files are derived, private, atomically published after ledger `fsync`, and rebuildable by a bounded-memory streaming scan.
- Complete-record truncation/corruption quarantines the stream; only a newline-incomplete final fragment is repairable.
- Same-process, cross-instance, two-live-process tail races and a live lock past its stale interval passed. One of two processes committing the same expected tail wins; the other observes `stale_tail`.
- A 200-record derived-index rebuild retained one in-memory tail record and recovered idempotency without retaining event history.
- POSIX evidence state modes passed at `0700` directories and `0600` ledger files.
- `npm run verify:release`: 301 unit/security/property-style tests, 146 focused tests, build, lint, declaration variants, and packed consumer passed.
- Proven tier: same-host local filesystems honoring Node file/directory sync and private-mode operations. The implementation deliberately does not claim remote-filesystem ordering, lying hardware, or resilience to manual deletion of a valid individual derived idempotency file; those limitations are documented.
