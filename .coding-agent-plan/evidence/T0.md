# T0 evidence

Status: done

- Started from Terminal UI `d83d01432056470d17f42793bb83f6d7c93e595d`.
- Agents implementation commit: `f4ff3b7a5dd87067c6b028ba5e58a65f244400d1`.
- Coding-agent manifest and workspace lock now resolve exact Terminal UI commit `d83d01432056470d17f42793bb83f6d7c93e595d` through the ordinary Git dependency.
- Agents' Agent Core source guard was advanced to verified R0 commit `f3b182c3bb01b4b7f18eeb95229796da1877a21c`.
- The earlier completion evidence is invalid. A clean `npm ci` rebuilt Terminal UI from the pinned source and exposed that Coding Agent still imported removed pre-migration APIs; the prior pass had resolved stale generated package output.
- Reopened at agents ledger commit `f0c0873ba14aa5c30f5fb8b36f22c3b7912e2814`. Completion now requires a clean install, source migration without aliases, and the complete Agents release gate.
- Coding Agent was migrated directly to the current component vocabulary and contracts: semantic callbacks, controlled `state`/`view`, scroll requests, collection ownership, source sinks, `TuiInitialResult`, and the object-form `runTui()` boundary. No compatibility alias or local dependency override was added.
- The migration exposed three consumer-owned defects: JavaScript tests still invoked the removed three-argument run boundary, the message composer omitted the required accessible textbox name, and resize rendering did not commit normalized conversation scroll state. All three are corrected in agents commit `d4cfcf215dd43e14c4f7cae1e510139b036b4d8e`.
- The E0 consumer migration is included in the same commit: provider egress preserves declared request-recovery capabilities, tool activity presents the exact recovery capability, and packed-consumer assembly includes `@agent-core/effects`.
- Clean `npm ci` completed from the Git-pinned Terminal UI dependency. Agents `npm run verify:release` then passed: plan validation, build, lint, 37 tests, and packed consumer installation/execution.
- Terminal UI `npm test` passed at exact commit `d83d01432056470d17f42793bb83f6d7c93e595d`: architecture/API/type/example/acceptance/conformance/integration/package/property/security/unit gates, 1,394 unit-lane tests, Node/Deno/Bun packed/runtime checks, JSR dry-run, and 41 performance contracts.
- The focused Coding Agent interaction evidence covers input and composer editing, progress while effects run, tool activity, approval and dismissal, command/search overlays, pointer/keyboard routing through Terminal UI, terminal resize through application state, bounded conversation display, session result projection, and shutdown. No Terminal UI-owned defect remained after correcting the consumer contracts.
