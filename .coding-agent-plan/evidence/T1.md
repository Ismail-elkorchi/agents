# T1 — Close confirmed Terminal UI consumer findings

Status: current

- T1 starts after A7 completed at Agents source `a644906c060c1935911283f153126dc863b56ac3` and Terminal UI `01277f04674c5f021cbe93dccfe378bacec7b632`.
- The first action is a latest-public-API ownership audit of Coding Agent's TUI composition for private imports, casts, duplicate runtime/focus/event/collection behavior, timing workarounds, and local patches. Coding-specific view composition remains in Agents; only reproduced general framework defects may change Terminal UI.
- A clean fetch confirmed Terminal UI `origin/main` is still exactly `01277f04674c5f021cbe93dccfe378bacec7b632`. Coding Agent imports only declared public package entry points. Its stream bridge uses the public reliable/replaceable source contract and framework backpressure; focus and modal dismissal use public dialog/focus policy; scroll virtualization uses the public retained measured collection/window; resize is an application message; and no timer, timing sleep, private import, type coercion around a framework boundary, or duplicate framework state machine was found.
- One apparent duplicate source-retirement line was rejected as an inspection-output overlap after checking exact numbered source; no repository edit was made. This ownership check prevented a manufactured compatibility or cleanup patch where no defect existed.
- Coding Agent's manifest and lockfile already resolve the exact reviewed Terminal UI commit. Agents release run [33179934919](https://github.com/Ismail-elkorchi/agents/actions/runs/33179934919) passed on all four release jobs with that pin, and Terminal UI CI run [33173915241](https://github.com/Ismail-elkorchi/terminal-ui/actions/runs/33173915241) passed at the same framework head.
- The complete Terminal UI `npm test` gate passed again without source changes: architecture and public API checks, lint and type consumers, acceptance/conformance/integration/package/property/security lanes, 1,395 unit tests, packed Node/Deno/Bun consumers, Node/Deno/Bun runtime smoke, JSR dry-run, and 41 performance contracts. T1 is complete without a dependency bump because latest public source equals the already pinned, pushed, verified commit.
