# V0 — First resilient CLI slice and stop/go gate

Status: done

- V0 composes Agents `beaf00120d78ac1ec8070777becdc6c08ec9e131`, Agent Core `616a50d67bdd82a6691ab8dfcdbf67609460140d`, Sandbox `e82ff3dd94a7c87b14bd25665b8898ae3b081767`, and Terminal UI `d83d01432056470d17f42793bb83f6d7c93e595d`.
- Before product integration, the baseline was restored across the release matrix. Agent Core now distinguishes the Windows process-termination tier from unsupported directory-entry power-loss durability, and non-Linux CI no longer invokes Linux-only root-capability process tests. Agents CI now checks out complete Agents history plus the exact reviewed Agent Core and Terminal UI repositories, and the plan validator proves that package pins equal reviewed heads; a depth-one checkout can no longer masquerade as failed ancestry or omit a repository the validator is required to inspect.
- A final Windows-only flake was traced to a test that coupled an authoritative 12 ms stream-idle timeout to delivery of a best-effort 2 ms status cadence. Agent Core `616a50d67bdd82a6691ab8dfcdbf67609460140d` keeps the timeout contract and removes the invalid scheduling assumption rather than delaying or weakening timeout behavior.
- The preceding Agents CI correction completes source materialization by checking out, building, and validating Sandbox as well as Agent Core and Terminal UI. Its generated-ledger check compares canonical content independently of Git's Windows CRLF checkout transformation.
- Agents `9e33fecdac7f2941747c31d332a9c6defafba1bb` adds an explicit taskless `coding-agent exec --resume`/`--session` path. It opens an existing session, restores and drives accepted work, and never creates another submission. A completed session is rejected as having no unfinished operation; a killed request with an unknown provider outcome is surfaced as a recovery suspension and is not replayed.
- The deterministic scripted-provider fixture kills the first process before the provider-effect intent, resumes the same accepted operation, applies root and nested `AGENTS.md` scopes, reads through the adopted workspace capability, performs one hash-bound structured edit, preserves an unrelated file, and emits the A5 machine-derived change report. The provider receives three logical turns, proving that recovery did not enqueue a second task.
- The complete scripted slice additionally includes a direct sandboxed command and a required isolated check. The ordinary host truthfully reports `linux-namespace-v1` unavailable (`required namespaces: Operation not permitted`), so there is no ambient fallback. The unmodified test was also run in a disposable privileged Linux container using Node 24 image digest `sha256:be23f54a88d34e8824c741b19b91064094f92c1c97b194144bfc8b50d67258e2`; Sandbox reported namespaces, Landlock ABI 8, seccomp, network namespaces, `mount_setattr`, and `execveat`, and all four resilience tests passed.
- That capable run exposed one real policy bug: Coding Agent treated every unsatisfied optional fact in Sandbox's complete enforcement report as a rejected preparation. Agents `beaf00120d78ac1ec8070777becdc6c08ec9e131` validates exactly `LINUX_PROCESS_BASELINE_REQUIREMENTS.required`; every declared baseline guarantee must be present and satisfied, while unrequested optional resource facts do not silently become mandatory.
- A real OpenAI Codex provider run completed through the same Coding Agent composition at run `ad80d4c0-aa6c-4eae-a7f4-8f52b7a02685`, session `11886246-63c8-44e9-89b2-7d5c11f19db0`, using `gpt-5.6-luna`. It performed a root-bound `package.json` read and returned the observed package name without workspace changes. Credentials, request content, and provider output are intentionally absent from this ledger.
- V0 exposed two platform-boundary defects and corrected their owners. Private-state paths now adopt the platform's canonical path instead of rejecting ordinary `/var` or Windows short-name aliases, while an alias resolving inside the workspace is rejected before any marker or directory is created. Non-Linux Agents CI excludes application tests that require the Linux-only root capability and retains a portable assertion that root adoption fails closed on those platforms.
- Local `npm run verify:release` passed at `beaf00120d78ac1ec8070777becdc6c08ec9e131`: plan validation, clean build, lint, 71 tests (67 passed and four explicitly capability-gated on the ordinary host), and packed Coding/Writing Agent consumers. GitHub run `33163078234` passed Ubuntu minimum/current Node 24, macOS, and Windows for the preceding source-complete commit; the one-line required-guarantee correction is covered by the same local gate and the capable-container slice.

## Stop/go decision

**Go.** V0 composes the existing Core, Sandbox, root-capability, repository-policy, verification, and change-report authorities without casts, duplicated durable state, a second recovery engine, or ambient execution. The two integration defects were corrected in their owning admission policies. Parallel settlement, candidate disposition, and complete TUI recovery may proceed from the current contracts.
