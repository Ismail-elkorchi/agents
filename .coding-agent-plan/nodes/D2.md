# D2 — Per-call durable tool settlement

**Intent:** Make one sequential tool call safe before adding parallelism.

**Work:** Persist planned call/input/tool implementation/effects, issue and consume one E0 ticket, then durably settle the complete normalized observation, evidence/artifact receipts, usage/exposure, and per-call state immediately on completion. Conversation projection is a later idempotent transition.

**Acceptance:** Kill at prepare/authorize/intent/start/external-complete/settlement/projection boundaries; completed effects never rerun; unknown effects never auto-replay; preconditioned reads only reexecute when stable; buffered mutation receipts reconcile; missing/mismatched tool implementations suspend explicitly; no preparation or resource lease leaks.
