# F0 — Fault, process-kill, and live stale-owner conformance

**Intent:** Prove the durability claims with real boundaries, not only reducer tests.

**Matrix:** Kill/fault before and after R0 append/sync/index steps; operation acceptance; driver claim/takeover; provider intent/remote completion/settlement; each parallel tool intent/start/completion/settlement/projection; approval; check intent/result; disposition; finalization prepare/session projection/`run.ended`; sandbox result/cleanup receipt; session submission settlement.

Run live two-process tests where the old driver remains alive and paused (a) before ticket start, (b) inside a queryable effect, (c) after external completion before settlement, and (d) while attempting a late transition. Prove it cannot start new work or commit unrelated state, the replacement does not execute concurrently, and exact late settlement is accepted at most once.

**Acceptance:** After reopen: no contradictory terminal truth, repeated settled effect, unsafe replay, missing durable settlement, invented success, unbounded recovery, or ambiguous next action. Run memory/JSONL and supported OS/sandbox tiers. Faulted/quarantined storage starts no effects.
