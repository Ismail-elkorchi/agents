# T0 — Adopt current Terminal UI and establish a consumer baseline

**Intent:** Remove the stale dependency before using TUI evidence.

**Sequence:** Fetch clean Terminal UI `origin/main`, run its complete gate, update manifest and lockfile through the package manager to the exact pushed commit, then build/test coding-agent without TUI source edits. Record compile/runtime migration failures and classify them by ownership.

**Acceptance:** Manifest and lock resolve the same exact commit; no local path override exists; Terminal UI and agents checks pass; a focused transcript covers input, streaming, tool activity, approval, session restore, resize/tiny bounds, pointer/keyboard, and shutdown; every discovered issue has a minimal owner-classification result.
