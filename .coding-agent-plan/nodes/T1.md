# T1 — Close confirmed Terminal UI consumer findings

**Intent:** Finish only general framework issues exposed by the completed product.

For each issue: reproduce against the latest public Terminal UI API, decide owner, add framework regression if owned there, correct the root contract without compatibility, run the full Terminal UI gate, push `main`, update coding-agent's exact dependency and lockfile, remove any temporary consumer code, and rerun agents. Coding-specific view composition remains in agents.

**Acceptance:** No private import, cast, duplicate runtime/focus/event/collection behavior, timing workaround, or local patch masks a Terminal UI defect. Final dependency resolves the recorded pushed commit.
