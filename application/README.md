# Application delivery

Bounded, disposable subscriptions for headless application services. Notifications are derived delivery, not durable execution records. A delivery gap requires an authoritative application read. Detaching a subscriber never cancels application work.
